@using Strem.Flows.Default.Flows.Triggers.Data

@inherits CustomTriggerComponent<OnEventRaisedTriggerData>

<div class="field">
    <label class="label">Interval Value</label>
    <div class="control">
        <input class="input" type="text" placeholder="i.e user-redeemed-points" @bind="Data.EventName">
    </div>
</div>

@code {
    public override string Title => $"On Event <strong>{Data.EventName}</strong> Raised";
}